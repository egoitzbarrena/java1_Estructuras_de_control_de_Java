/*
 * Programa que obliga al usuario a introducir un número menor que 100
 */
//importa todas las clases  del paquete java.util
import java.util.*;
//definición de la clase Ejemplo1DoWhile
public class Ejemplo1DoWhile {
//define el método main
    public static void main(String[] args) {
//define la variable de tipo integer
        int valor;
//utiliza la clase scanner para recojer todo lo que el usuario meta por el teclado y lo guarda en la variable in de tipo Scanner.
        Scanner in = new Scanner( System.in );
//mientras el numero que esta en la variable valor sea mayor a 100 nos seguira pidiendo que introduzcamos otro numero, si no se aparecera el numero.
//Primeramente se ejecuta la sentencia y luego evalúa la condició.

        do {
//pinta en la pantalla un texto. 
            System.out.print("Escribe un entero < 100: ");
//recoje lo que has introducido en la variable valor.
            valor = in.nextInt();
           }
//mientras el numero sea mayor o igual a 100 va a ejecutar las siguientes lineas.
	while (valor >= 100);
//pinta en la pantalla un texto. la frase has introducido y el valor de la variable valor.
        System.out.println("Ha introducido: " + valor);
    }
}
